# Conversion Notes

This bundle was generated directly from the live StemStudio scene "Pirate Ship Battle Royal v1.0 (Clone)" (6a023b1972261e6d4fab20f6).

## Known Gaps

- Skipped non-default DirectionalLight "Directional Light" because the Script Tool export only recreates the default camera and default lights.
- Skipped unsupported object "GlobalBehaviorsHost" (Object3D) because it is neither a model instance, a group, nor a supported primitive.
- Object "OceanSurface" has direct texture map assignments. The exporter preserved the texture asset files, but Script Tool replay for direct material textures still needs manual relinking.
- Behavior "exported.cannonturret" on "cannon-mobile.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shiphealth" on "ship-pirate-large.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shipcontroller" on "ship-pirate-large.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "character" on "ship-pirate-large.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shipnameplate" on "ship-pirate-large.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shiphealth" on "ship-pirate-medium.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shipnameplate" on "ship-pirate-medium.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.aishipcontroller" on "ship-pirate-medium.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shiphealth" on "ship-ghost.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shipnameplate" on "ship-ghost.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.aishipcontroller" on "ship-ghost.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shiphealth" on "ship-small.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.aishipcontroller" on "ship-small.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shipnameplate" on "ship-small.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.cannonturret" on "cannon.glb" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.gamemanager" on "GameHost" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.piratehud" on "HUDHost" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shiphealth" on "ship-pirate-medium.glb 3" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shipnameplate" on "ship-pirate-medium.glb 3" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.aishipcontroller" on "ship-pirate-medium.glb 3" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shiphealth" on "ship-ghost.glb 3" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shipnameplate" on "ship-ghost.glb 3" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.aishipcontroller" on "ship-ghost.glb 3" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shiphealth" on "ship-small.glb 3" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shipnameplate" on "ship-small.glb 3" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.aishipcontroller" on "ship-small.glb 3" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shiphealth" on "ship-pirate-medium.glb 4" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shipnameplate" on "ship-pirate-medium.glb 4" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.aishipcontroller" on "ship-pirate-medium.glb 4" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shiphealth" on "ship-ghost.glb 4" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shipnameplate" on "ship-ghost.glb 4" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.aishipcontroller" on "ship-ghost.glb 4" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shiphealth" on "ship-small.glb 4" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.shipnameplate" on "ship-small.glb 4" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.aishipcontroller" on "ship-small.glb 4" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
- Behavior "exported.cannonturret" on "cannon-mobile.glb 2" uses throttle settings that are not currently exported through Script Tool commands. Reapply them manually if needed.
